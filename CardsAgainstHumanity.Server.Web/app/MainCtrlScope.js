var App;
(function (App) {
    var MainCtrlScope = (function () {
        function MainCtrlScope() {
        }
        return MainCtrlScope;
    }());
    App.MainCtrlScope = MainCtrlScope;
})(App || (App = {}));
